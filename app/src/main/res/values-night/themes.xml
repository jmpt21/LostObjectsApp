<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.LostObjectsApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <item name="colorPrimary">@color/dark_primary</item>
    </style>

    <style name="ThemeAppBar" parent="Base.Theme.LostObjectsApp">
        <item name="colorPrimary">@color/dark_primary</item>
        <item name="android:backgroundTint">@color/dark_primary</item>
        <item name="android:text">@color/white</item>
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:textColor">@color/white</item>
        <item name="colorControlNormal">@color/white</item>
        <item name="controlBackground">@color/dark_primary</item>
        <item name="tabIndicatorColor">@color/white</item>
        <item name="tabSelectedTextColor">@color/white</item>
        <item name="tabTextColor">@color/white</item>
    </style>

    <style name="ThemeFAB" parent="Base.Theme.LostObjectsApp">
        <item name="colorPrimary">@color/dark_primary</item>
        <item name="android:backgroundTint">@color/dark_primary</item>
    </style>
</resources>